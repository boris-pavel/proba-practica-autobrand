spring:
  datasource:
    url: jdbc:postgresql://localhost:55432/autobrand   # ← schimbat de la 5432
    username: autobrand
    password: dev_password
    driver-class-name: org.postgresql.Driver

app:
  scraping:
    cron-enabled: false